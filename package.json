{
  "name": "@antimatter/source",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "infrastructure"],
  "scripts": {
    "test": "npm --workspaces run test --if-present",
    "build": "npm --workspaces run build --if-present",
    "lint": "npm --workspaces run lint --if-present"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "@types/node": "^22.0.0",
    "typescript": "~5.9.2",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
